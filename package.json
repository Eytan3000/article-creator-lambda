{
  "name": "article-creator-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda boilerplate with TypeScript (regular build)",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepackage": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.700.0",
    "@sanity/client": "^7.16.0",
    "openai": "^6.25.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
